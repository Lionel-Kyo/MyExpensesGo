import Typography from "@mui/material/Typography";

const NotFound = (props: any) => {  
	return (
		<Typography variant="h1" color="primary" align="center" {...props}>
		NOT FOUND
		</Typography>
	);
}

export default NotFound;